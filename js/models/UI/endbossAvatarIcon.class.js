class EndbossAvatarIcon extends DrawableObjects {

    IMAGE = ['img/6.AvatarsAndIcons/Icons/PNG/Background/con11.png']

    x = 920;
    y = 30;
    height = 80;
    width = 85;

    constructor() {
        super().loadImage(this.IMAGE);

    }
}